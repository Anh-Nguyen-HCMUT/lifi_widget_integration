# This project is for Lifi Integration testing only

## Install dependencies
`npm install`

### Run the app in the development mode
`npm start`
Open [http://localhost:3000](http://localhost:3000) to view it in the browser.